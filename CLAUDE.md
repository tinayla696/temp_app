# CLAUDE.md

最初に `@AGENTS.md` を読むこと。本ファイルは Claude Code 固有の補足のみを書く。

## 作業の粒度

- 1つの PR で完結する粒度に分ける。大きい依頼は、着手前に分割案を提示して合意を取る。
- 既存ファイルは差分編集する。全文を書き直さない（改行コードの差分が全行に広がるため）。

## 実行環境

- ローカル PC（Windows）と GCP 開発 VM（Ubuntu）の両方で動かす。
  OS 固有のパスやコマンドを手順に書かない。書く必要がある場合は両方を併記する。
- 仮想環境は `.venv` を使う。グローバルに pip install しない。

## 報告

- `mkdocs build --strict` の実行結果を報告に含める。通していない場合はその旨を明記する。
- 確認できていないこと（未実行のテスト、推測で書いた箇所）を必ず書く。

## 権限

- `.claude/settings.json` の設定に従う。deny 対象の操作は実行せず、提案として提示する。
- `.github/workflows/` の編集と push は人間の確認を得てから。

## コミット

- メッセージは AGENTS.md の規約（`type(scope): subject`）に従う。
- 変更していないファイルをステージしない。`git add -A` ではなくパスを指定する。
